# AWS Configuration
AWS_REGION=us-east-1
S3_BUCKET=sensor-data-demo

# Optional: Explicit AWS credentials (not recommended, use ~/.aws instead)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
